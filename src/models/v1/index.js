const UserModel = require("./users.model");
const AccountsModel = require("./accounts.model");
const TransactionsModel = require("./transactions.model");

const V1_MODELS = {
  UserModel,
  AccountsModel,
  TransactionsModel,
};

module.exports = V1_MODELS;
